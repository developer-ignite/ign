Create a block named Event Hero

It will be inspired on the /var/www/ign/wp-content/themes/ign/.taktician-project/reports/blocks/post-hero block, but for events from the events calendar plugin.

It should show the event title, start and end date and time (if the start and end date are the same show only once), the location name, the excerpt, a button if it has an external link...

make me proud